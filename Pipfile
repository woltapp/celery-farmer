[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
pytest = "*"
flake8 = "*"
pytest-cov = "*"
mypy = "*"

[packages]
farmer = {editable = true,path = "."}

[requires]
python_version = "3.7"

[scripts]
all-checks = "$SHELL -c 'flake8 && mypy src . && pytest'"
lint = "flake8"
test = "pytest"
typecheck = "mypy src ."
